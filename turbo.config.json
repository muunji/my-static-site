{
  "$schema": "https://turbo.build/schema.json",
  "global": {
    "externalDependencies": ["node_modules/**"]
  },
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}